# 熊本之旅

純前端旅遊行程手冊（Vite + React + TypeScript）。資料存瀏覽器 localStorage。

## 本機開發

```bash
npm install
npm run dev -- --host
```

手機同 Wi‑Fi 開啟終端機顯示的 Network 網址。

## 建置

```bash
npm run build
npm run preview -- --host
```

## 功能摘要

總覽／每日行程（編輯、排序、Google 導航、距離地圖）／行李／預算／日文翻譯；規劃⇄旅途模式；JSON 備份匯入匯出。
